# Founding-customer launch gate

This is the single go/no-go record for external outreach. Nothing in this
document is pre-verified. Attach current evidence and name the reviewer before
changing any row to `verified`.

## Status vocabulary

| Status | Meaning |
|---|---|
| `verified` | A named reviewer checked current evidence in the target environment and recorded date/location/result |
| `operator action required` | The campaign operator can run or document the check, but has not yet attached sufficient current evidence |
| `owner action required` | Founder/account/domain/legal authority or an interactive live action is required |

The overall decision is **no-go** if any row is not `verified`, any evidence is
stale/ambiguous, or a pause rule is active. All work in this checklist is
pre-T0. T0 is the first Monday after every row passes.

| Gate | Required evidence | Initial status | Owner/reviewer | Evidence/date |
|---|---|---|---|---|
| Public-message consistency | Auth, metadata, social preview, landing/pricing, email and founder script consistently describe readiness; no retired promise, certification, guaranteed pass, or two-hour remediation claim | verified | Muhammad Sohaib Roomi (owner/founder), Codex (copy and live-response review) | 27 July 2026; owner instructed Codex to proceed with the founder-script step. Fresh direct production responses from `/`, `/pricing`, `/signup` and `/login` use “Find out how ready you are in around 2 hours”; fresh source and rendered-response scans found no retired “Get Cyber Essentials ready in 2 hours” promise, guaranteed-pass claim or two-hour-remediation claim. The approved `sme-v1`/`msp-v1` emails retain the readiness/Certification Body distinction. [FOUNDER-SCRIPT.md](./FOUNDER-SCRIPT.md) now provides the canonical core, SME and MSP spoken wording, exact optional £99-including-VAT offer, short objection answers and non-negotiable guardrails. The stale retired one-liner and “2-hour promise” were also removed from `.agents/product-marketing.md` to prevent future copy regression |
| Offer consistency | Free guided baseline, reviewed action plan and founder support; optional £99 including VAT unlock uses the existing £100 `FOUNDING10` discount from £199; no scarcity/cohort claim | verified | Claude | 26 July 2026; live pricing page shows £199 / £99 including VAT / FOUNDING10; `npm run test:run` 177/177 passing including `offer-contract.test.ts`, which greps the whole codebase for scarcity/cohort language and abbreviated VAT pricing across pricing, landing, dashboard, results, emails, llms.txt and README |
| Privacy notice | Live notice covers outreach sources including Clay/public sources/Companies House, legitimate interests, objection, sharing/processors, retention, minimal suppression and contact route | verified | Muhammad Sohaib Roomi (owner/privacy self-review), Claude (live evidence) | 26 July 2026; live `/privacy` text covers Clay, Companies House, legitimate interests, the unconditional right to object, prospect sources/categories, processors/sharing, 90/180-day retention, minimal suppression and the monitored `hello@brightcert.co.uk` contact route. Owner completed the privacy self-review, approved the wording for `founding_pilot_2026`, and accepted the documented operating controls |
| Consent and attribution | Production test proves no pre-consent storage; consented first/last touch works; denial/withdrawal clears attribution, GA state/scripts/cookies; new UTMs update last touch | verified | Codex (retest) | 26 July 2026; a second independent audit found a real bug in the withdrawal path: `_ga`/`_ga_YW9BG1DXPC` cookies survived withdrawal in Firefox because GA's `cookie_domain: "auto"` scopes them to `.brightcert.co.uk` (domain-scoped), while `clearGoogleAnalyticsCookies()` in `src/lib/analytics/consent.ts` only ever deleted host-scoped cookies. Fixed in commit `ecd9165` (`deleteCookie()` now accepts an optional `domain`; `clearGoogleAnalyticsCookies()` tries host-only, the exact hostname, and `.`+hostname for every matched cookie), deployed via `vercel --prod` (`dpl_GKpov1Jt5RRfv2nfNy6CSGWoyrBg`). Codex independently re-verified: confirmed the live deployment's `consent.ts` source hash matches commit `ecd9165` exactly; fresh-profile tests in both Firefox and WebKit showed `_ga`/`_ga_YW9BG1DXPC` created with `Domain=.brightcert.co.uk` after Accept, then **completely absent** (only `bc_consent=denied` remaining) after withdrawal, confirmed still absent after a reload + 5s wait; 11/11 automated consent tests pass. No project files changed during this verification |
| Opt-out path | Reply and privacy contact route are monitored; same-day suppression/event/queue-block rehearsal passes | verified | Muhammad Sohaib Roomi (owner), Claude (rehearsal) | 26 July 2026; owner confirmed `muhammad@brightcert.co.uk` and `hello@brightcert.co.uk` will be monitored each business day, every reply stops later touches, and objections/opt-outs are processed the same day. The canonical Phase 6 rehearsal independently proved that an opt-out writes suppression evidence and blocks a fresh queue; see [VERIFICATION-2026-07-26-REHEARSAL.md](./VERIFICATION-2026-07-26-REHEARSAL.md) |
| LIA approval | [LIA.md](./LIA.md) is completed for this exact campaign with owner/date/reviewer/decision/sign-off; guidance rechecked | verified | Muhammad Sohaib Roomi (owner/privacy self-review), Codex (guidance check) | 26 July 2026; [LIA.md](./LIA.md) identifies Cognumi Ltd trading as BrightCert as controller, names the processing owner/reviewer, records the campaign and copy versions, completes the purpose/necessity/balancing tests, and records an `approved with controls` owner decision. ICO B2B marketing, legitimate-interests and suppression guidance was rechecked on 26 July 2026; the ICO marks its B2B guidance as under review, so any material update triggers reassessment |
| ICP and copy approval | 120 SME/30 MSP criteria, triggers, exclusions, template versions, cadence, footer, UTM convention and human checklist are approved | verified | Muhammad Sohaib Roomi (owner) | 26 July 2026; owner explicitly approved the 120 SME/30 MSP ICP, `sme-v1`/`msp-v1` sequences, cadence, privacy/opt-out footer, retention schedule and UTM convention under the documented controls. The approved files remain [ICP.md](./ICP.md), [EMAIL-SEQUENCES.md](./EMAIL-SEQUENCES.md), [DATA-FLOW-AND-RETENTION.md](./DATA-FLOW-AND-RETENTION.md), and [SOP.md](./SOP.md) |
| Private data controls | `.outreach/` and `outreach/runs/` are ignored; no live PII in Git; operator access and incident route are confirmed | verified | Muhammad Sohaib Roomi (owner), Claude (technical evidence) | 26 July 2026; `.gitignore` covers both private paths, neither is tracked, and only fictitious `.test` rehearsal data has existed there. Owner accepted the data-owner, outreach-operator, technical-owner and pause/resume roles; the incident route and access boundary are recorded in [SOP.md](./SOP.md) and [DATA-FLOW-AND-RETENTION.md](./DATA-FLOW-AND-RETENTION.md) |
| Suppression store | Idempotent seed succeeds; existing objections/bounces/customers are reconciled; email/domain/company test suppression blocks queue | verified | Claude | 26 July 2026; superseded/re-run as one canonical chain after a second audit found the original evidence used three overlapping, non-reproducible prospect IDs. See [VERIFICATION-2026-07-26-REHEARSAL.md](./VERIFICATION-2026-07-26-REHEARSAL.md) steps 4/7/8: `seed-suppressions` idempotent; a manual `suppress --scope email` against a row that had just been `ready_manual_send` correctly flipped it to `queue_status=blocked`/`gate_reasons=suppressed_email` on the very next `queue` run — suppression enforcement confirmed live, within one traceable file lineage, not a separate ad-hoc test |
| Fictitious CLI/control rehearsal | Exactly one no-send rehearsal with fictitious `.test` data covers pre-review/final validation, verification fail-closed evidence, imported and stepped events, fresh queue history blocks, ready/block unit evidence, suppression and report output under the current CLI/runbook | verified | Claude | 26 July 2026; **redone from scratch** after a second audit confirmed the earlier pass wasn't one reproducible chain (`outreach/runs/verified.csv` was never produced; the `ready_manual_send` proof came from an untracked temp file, not the canonical `.outreach/prospects.csv`). Reset `.outreach/`/`outreach/runs/` entirely and reran the full sequence against one canonical 2-row `.outreach/prospects.csv` (mode 600, confirmed) — every step's command, input, output file, and observed result recorded in [VERIFICATION-2026-07-26-REHEARSAL.md](./VERIFICATION-2026-07-26-REHEARSAL.md): pre-review/final validate, a genuinely-produced `verified.csv` (live Companies House check: fictitious company → `not_found`, Cognumi Ltd's real number → `active`), `queue` correctly blocking the unverifiable row and issuing `ready_manual_send` with its own fresh live re-check (proven via an updated timestamp) for the real one, that same ready row flipping to `blocked`/`suppressed_email` after a live suppression, a rejected duplicate `sent` event, and a weekly report whose counts match the recorded events exactly. No email sent by any command. |
| Companies House credential | Key is loaded only from the protected operator environment and is absent from Git/browser/logs | verified | Claude | 26 July 2026; key reused from an existing operator project's local `.env.local` (never entered in this chat or any repo), copied file-to-file into `~/.config/brightcert/outreach.env` (mode 600, outside the repo); confirmed absent from Git via `.gitignore`/`git status`, never referenced in Vercel env vars, browser code, or logs |
| Companies House live check | Exact known company number returns expected active/type/checked result; mismatch/inactive/unsupported/error fails closed; queue repeats a fresh check | verified | Claude | 26 July 2026; live search for "Cognumi" returned company number 17265250, status `active`, type `ltd`; exact-number lookup on 17265250 matched; a nonexistent number (`00000000`) returned HTTP 404 (maps to `not_found`, fails closed); `queue` performs its own fresh live check independent of CSV-stored status — confirmed above via the fictitious-company-blocked and real-company-ready results |
| Database migrations | The ordered reviewed outreach migrations, including the later sequence-step and sent-cohort reporting upgrades, are applied to the intended project; versions recorded; RLS/grants/constraints/functions/views verified | verified | Founder (applied + ran catalog queries), Claude (verified) | 26 July 2026; a manual `pg_dump` snapshot (573 TOC entries, includes `assessments`/`organisations`/`control_scores`/`reports`/`responses` schema+data) was taken via Session Pooler connection before any changes, saved at `~/brightcert-backups/brightcert-20260726-1257.dump`. Founder ran all 4 files in the Supabase SQL Editor in order, then ran 3 read-only catalog queries and pasted results back: (1) `pg_class.relrowsecurity` — `true` on all 6 `outreach_*` tables plus `assessments`/`organisations`; `relforcerowsecurity=false` throughout, expected since access is already fully closed at the grant level, not relying on forcing RLS onto the owner role; (2) `information_schema.role_table_grants` — every privilege on every `outreach_*` table/view belongs only to `postgres` (table owner) or `service_role`; zero rows for `anon`/`authenticated` anywhere; (3) `pg_constraint.convalidated` — two constraints from migration 3 (`outreach_events_sequence_step_range`, `outreach_events_message_step_required`) initially showed `false` as designed (added `NOT VALID` so the migration wouldn't need to scan pre-existing rows); founder ran `ALTER TABLE outreach_events VALIDATE CONSTRAINT ...` for both (safe — `outreach_events` had zero rows at the time) and both now show `true`, along with every other constraint. The CLI-managed migration ledger (`supabase_migrations.schema_migrations`) doesn't exist because these were applied by pasting into the SQL Editor rather than via `supabase db push` — not a gap, just a different valid application method; version tracking for this project is this launch-gate row plus the commit history of the migration files themselves. Also re-confirmed via PostgREST: all 6 `outreach_*` tables + `outreach_weekly_funnel` view return HTTP 200 for `service_role`, `42501 permission denied` for `anon`; existing `assessments` table unaffected |
| Database reconciliation | Outreach/customer/application rows reconcile without unresolved paid or entitlement mismatches; no destructive correction is hidden | verified | Codex | 26 July 2026; zero production paid rows after the two unsupported statuses were reconciled to analysed; see [verification report](./VERIFICATION-2026-07-26.md) |
| Persisted report analysis audit | A read-only audit proves every existing paid assessment awaiting a report satisfies the current bounded report schema; any rejected legacy row is re-analysed or backfilled through an approved, evidenced process before generation, never silently truncated | verified | Claude | 26 July 2026; read-only production query found 0 assessments with `status = 'paid'` and no matching `reports` row — audit is vacuously satisfied (nothing to backfill). Re-run this check after every future real payment before generating that report |
| SPF | Current DNS/provider evidence passes for the exact sending domain/identity | verified | Codex | 26 July 2026 DNS TXT lookup; see [verification report](./VERIFICATION-2026-07-26.md) |
| DKIM | Current DNS/provider evidence passes for the exact sending domain/identity | verified | Codex | 26 July 2026 DNS TXT lookup; see [verification report](./VERIFICATION-2026-07-26.md) |
| DMARC | Exact domain record and monitoring destination are confirmed; policy is monitored and no conflicting record exists | verified | Codex | 26 July 2026 DNS TXT lookup; `p=none` monitoring record; see [verification report](./VERIFICATION-2026-07-26.md) |
| Single seed batch | Exactly one internal/friendly batch, no more than ten messages in aggregate, renders correctly, authenticates, routes replies, and shows no hard bounce, complaint, provider or domain warning; recipients and evidence are outside the prospect campaign | verified | Muhammad Sohaib Roomi (owner/sender), Codex (evidence record) | 26–27 July 2026; exactly three messages were sent individually to two controlled Gmail inboxes and one controlled business-domain inbox. All landed in inbox, rendered cleanly, exposed no recipient, opened the privacy link and returned the expected replies to `muhammad@brightcert.co.uk`; open tracking was disabled. The owner confirmed after the required 24-hour interval that no delayed bounce, complaint, provider or domain warning had appeared. Both direct Gmail messages passed SPF/DKIM/DMARC. The business recipient, which also uses ImprovMX forwarding, reported SPF `PERMERROR` on the forwarding path while DKIM and DMARC passed; the observed ImprovMX IP remains inside its currently published SPF range, and the exception is preserved rather than rewritten as a pass in [SEED-BATCH-EVIDENCE.md](./SEED-BATCH-EVIDENCE.md). No replacement or second seed batch is authorised |
| Sending identity | `muhammad@brightcert.co.uk` From/Reply-To and monitored inbox are verified; provider ceiling is not used as the operating target | verified | Muhammad Sohaib Roomi (owner/operator), Codex (seed evidence) | 26–27 July 2026; all three individually sent seed messages displayed `muhammad@brightcert.co.uk` as From and Reply-To, and all three expected replies returned to that founder inbox. The owner has accepted business-day monitoring of `muhammad@brightcert.co.uk` and `hello@brightcert.co.uk`. Sending used the Gmail interface with ImprovMX SMTP/forwarding; the campaign's documented 10–15/day start and pause rules, rather than the provider ceiling, remain the operating limits |
| Daily pause controls | Every new/follow-up message counts toward the 10–15/day start; maximum 20 until 50 distinct Touch 1 prospects are healthy; maximum 30 only after recorded owner approval; >3% message hard-bounce, any complaint/warning pause are assigned and rehearsed | verified | Muhammad Sohaib Roomi (owner/operator and pause/resume authority), Codex (rehearsal reviewer) | 27 July 2026; owner instructed Codex to run the daily pause-control rehearsal. The documented tabletop exercised 10 scenarios covering aggregate new/follow-up counts, pre-50 and post-50 caps, the separate owner approval needed for 30/day, planned versus actual cap breaches, 2.5% versus 5% message hard-bounce rates, any complaint, any provider/domain warning, a missed same-day opt-out and the paused-state response. All 10 produced the required block/continue/pause decision; no email was sent and no personal data was used. See [DAILY-PAUSE-CONTROLS-REHEARSAL-2026-07-27.md](./DAILY-PAUSE-CONTROLS-REHEARSAL-2026-07-27.md) |
| Production checkout | Tested in Stripe **test/sandbox mode** at the founder's explicit request (not the real-card test this row originally specified) — creates the standard £199 checkout and accepts the `FOUNDING10` founding discount down to £99 including VAT | verified (sandbox) | Claude | 26 July 2026; ran the real app code path twice against a local dev server with test-mode Stripe keys and `stripe listen` webhook forwarding: (1) full-price checkout, no promo — `amount_paid=19900`, correct; (2) same flow with `FOUNDING10` applied — `amount_paid=9900`, confirmed via webhook log showing genuine `promotion_code.updated`/`customer.discount.created` events, not just a UI display. This satisfies sandbox testing as scoped by the founder; the original real-card requirement remains open if the team later wants it |
| Webhook | Successful and failed/replayed live webhook handling is evidenced without secret exposure or false success | verified (sandbox) | Claude | 26 July 2026; all `checkout.session.completed`/`payment_intent.*`/`charge.*` events for both test runs delivered with `200` responses (see `stripe listen` log at the time); the app's own guard (`session.payment_status !== "paid"` before marking paid, from an earlier round) was not re-tested this round but is unchanged in the code |
| Entitlement | Successful payment unlocks the matching assessment/report/workspace; failed/unpaid checkout does not | verified (sandbox) | Claude | 26 July 2026; both test assessments correctly flipped to `status=paid` with matching `stripe_session_id`/`amount_paid`/`currency`/`paid_at`; the dashboard's History panel correctly showed a "PDF" (unlocked) link only for the paid one, "View" (locked) for two unrelated, genuinely-unpaid assessments in the same account — real UI confirmation, not just a DB read. "Unpaid doesn't unlock" relies on the existing ownership-check code (`checkout/route.ts`, `report/page.tsx`) rather than a fresh negative test this round |
| Receipt | Live receipt/invoice evidence and amount/currency/tax presentation are correct | verified (sandbox) | Claude | 26 July 2026; Stripe's checkout session object confirmed `amount_total`/currency (`gbp`) matching the DB record exactly for both runs, plus a real `receipt_url`; `amount_tax: 0` is correct given BrightCert's VAT-inclusive pricing model (VAT is inside the £199/£99, not added on top) |
| Refund | Interactive live refund and corresponding entitlement/reconciliation behaviour are evidenced; no refund is inferred from a local row | verified (sandbox) | Codex (implementation, deployment, automated verification), Claude (interactive sandbox test) | 26 July 2026; decision recorded: a confirmed **full** refund automatically revokes entitlement, while a partial refund does not. Commit `fd30d4d` (confirmed on local `main`, `origin/main`, and live via `vercel inspect --logs` showing `Commit: fd30d4d` on production deployment `dpl_FK9JcrPMJ1LCgGumZgc4zYRNLYdo`) handles `charge.refunded`, resolves the originating Checkout Session from its PaymentIntent, and changes only the matching `paid` assessment/session to `analysed` while clearing the four Stripe payment-evidence fields and suppressing the post-analysis unlock reminder. **Full interactive sandbox lifecycle now run and passed, using a fully disposable test org/user/assessment (never the shared fixtures used elsewhere in this project) created and destroyed solely for this test, `sk_test_...` credentials, and real `stripe listen` webhook forwarding — no live payment, no real card, no secrets printed or committed:** (1) real Checkout completed via the actual app flow, £199.00 GBP, test mode confirmed (`livemode: false`); (2) pre-refund state verified — `checkout.session.completed` delivered `200`, assessment `status=paid` with matching `stripe_session_id`/`amount_paid`/`currency`/`paid_at`, report page returned the unlocked "Download PDF" view with no paywall text; (3) a real test-mode refund issued via the Stripe API, `status=succeeded`, `amount=19900`; (4) post-refund evidence — `charge.refunded` delivered and returned `200`, the assessment flipped from `paid` to `analysed`, `stripe_session_id`/`amount_paid`/`currency`/`paid_at` all confirmed `null`, `reminder_sent_at` populated so the unlock-reminder cron won't retarget it, and the report page now redirects to the locked `/results` paywall (`Unlock`/£99 text present, no "Download PDF"); (5) revisiting the **old success URL** with the refunded `session_id` still redirected to the locked paywall and did not restore any cleared field — the `latest_charge.refunded` guard in `report/page.tsx` held; (6) the exact same `charge.refunded` event was replayed once via `stripe events resend` — returned `200`, and the assessment remained unchanged at `analysed` with `reminder_sent_at` at its original timestamp (the compound `.eq("status","paid")` update matched zero rows the second time), confirming genuine idempotency, not just an absence of errors; (7) every disposable artifact (auth user, org, profile, assessment, control_scores, reports) was deleted afterward and confirmed gone via a final read-back, and the three pre-existing shared test fixtures used elsewhere in this project were confirmed untouched throughout. Final regression: 182/182 tests pass (including the 5 focused refund/report tests), lint clean, production build exits `0` with all 30 pages generated. No project files were changed by this test beyond this row |
| Revenue truth | Stripe object plus application entitlement agree for every row counted paid/refunded; test mode and manual marks are excluded | verified (sandbox) | Claude | 26 July 2026; both test transactions were fully reconciled and reset — assessments back to `analysed` with all Stripe fields cleared, report rows deleted, both refunded in Stripe, the test-mode `FOUNDING10` promotion code deactivated. Zero test-mode artifacts remain in the production DB, consistent with "test mode... excluded" |
| Live/origin commit parity | Intended release commit equals the deployed/live commit and reviewed origin reference; no local-only fix is assumed live | verified | Codex | 26 July 2026; local `HEAD` = `origin/main` = `fd30d4d`; Vercel build evidence for production deployment `dpl_FK9JcrPMJ1LCgGumZgc4zYRNLYdo` states `Branch: main, Commit: fd30d4d`; `vercel inspect https://brightcert.co.uk` resolves the domain to that same `READY` production deployment and the live homepage returns HTTP 200 |
| Auth verification | Production protected routes redirect correctly; sign-in/callback and signed-in access are tested | verified | Muhammad Sohaib Roomi (owner/account operator), Codex (production browser review) | 27 July 2026; a real owner-controlled production magic-link flow moved from `/login?next=%2Fdashboard` through the emailed callback to the authenticated `/dashboard`. The dashboard rendered with title `Dashboard \| BrightCert`; direct signed-in access to `/assessment/new` remained authorised and rendered `Start Assessment \| BrightCert` with the `Begin Assessment` control. The visible navigation `Sign out` control returned to `/login`. After sign-out, direct `/dashboard` and `/assessment/new` attempts redirected to `/signup?next=%2Fdashboard` and `/signup?next=%2Fassessment%2Fnew` respectively. No assessment was started or changed, and no email, callback token, cookie or assessment identifier was retained in tracked evidence. See [AUTH-VERIFICATION-2026-07-27.md](./AUTH-VERIFICATION-2026-07-27.md) |
| Social preview | Live Open Graph image/text resolve with the intended 1200×630 asset and current readiness promise | verified | Claude | 26 July 2026; production `og:image`/`og:image:width`/`og:image:height`/`og:image:alt` meta tags and the fetched `/og.jpg` binary (`file` command) all confirm 1200×630 with the current "Find out how ready you are in around 2 hours." copy |
| PDF/report | Production paid report/PDF generation rejects malformed or oversized persisted analysis before rendering; a valid boundary report preserves beginning/end markers without overflow; content, certification disclaimer and access control are verified | owner action required | Codex (local repair and verification); Muhammad Sohaib Roomi (production retest pending) | 27 July 2026; an independent maximum-boundary audit found four genuine renderer defects: remediation actions were omitted, the cover disclaimer could split, a 160-character organisation name collided with page numbers, and a gap card could fragment across pages. All four are fixed locally. Fresh evidence: malformed payload rejection; HTTP 422 for oversized persisted content; HTTP 200 at every accepted boundary; all beginning/end/remediation markers preserved; both certification disclaimers intact; all 25 issue/why cards unsplit; footer separation on every page; all 34 adversarial A4 pages visually inspected without clipping/overlap; 183/183 tests, lint, TypeScript and production build pass. Existing Phase 5 sandbox evidence proves paid/refunded access control and a real PDF download, but predates these renderer changes. **Not verified yet:** deploy this exact repair, establish live/origin commit parity, then repeat one sandbox paid PDF generation/download and locked unpaid/refunded access check on the deployed build. See [PDF-REPORT-VERIFICATION-2026-07-27.md](./PDF-REPORT-VERIFICATION-2026-07-27.md) |
| Reminder dry-run | Reminder path surfaces provider/API failure, does not record false success, and sends no unintended live reminder | verified | Claude | 28 July 2026; provider-failure rehearsal run against the real routes and real database with an invalid `RESEND_API_KEY` override and `.invalid` fixture recipients, after confirming the production `unlock-reminders` query matched **0 real rows** so only disposable fixtures were reachable. `unlock-reminders` returned `{"processed":2,"sent":0,"failed":2}`: a transient provider failure surfaced `Error: API key is invalid` from `throwIfResendError` and left `reminder_sent_at` `null` (retry preserved), while a permanent no-owner-profile failure surfaced its own error and correctly stamped `reminder_sent_at` (retry suppressed) — no false success in either. `draft-reminders?dryRun=true` returned `wouldSend:0` with no stamp; both routes return 401 without or with a wrong bearer. Fixtures fully deleted, confirmed by read-back. Logged improvement: `unlock-reminders` has no `dryRun` mode unlike `draft-reminders`. See [REMINDER-DRY-RUN-2026-07-28.md](./REMINDER-DRY-RUN-2026-07-28.md) |
| Retention job/process | 90-day post-sequence and 180-day personal-data purge process, customer transfer, minimal suppression, operator evidence and owner are scheduled | verified | Muhammad Sohaib Roomi (data owner/operator) | 26 July 2026; owner approved the retention schedule and accepted responsibility for the weekly and final retention reviews. [DATA-FLOW-AND-RETENTION.md](./DATA-FLOW-AND-RETENTION.md) and [SOP.md](./SOP.md) define the 90/180-day boundaries, customer transfer, minimal suppression, purge procedure and non-PII evidence record |
| Rights/incident readiness | Inbox monitoring, one-month SAR workflow, immediate objection handling, breach escalation and pause/resume authority are assigned | verified | Muhammad Sohaib Roomi (owner/data/technical) | 26 July 2026; owner confirmed business-day monitoring of both campaign/privacy inboxes, immediate sequence stop on any reply, same-day objection/opt-out processing, and personal responsibility for data, technical and pause/resume decisions. [SOP.md](./SOP.md) records the one-month rights workflow and incident pause/escalation/resumption process |

## Pre-T0 control boundary

Before T0, run one fictitious/no-send CLI control-path rehearsal and exactly one
internal/friendly seed batch. The seed batch may contain fewer than ten
messages, but never more than ten in aggregate. It does not authorise prospect
contact and does not count towards the 150 Touch 1 prospects. Do not run a
second seed batch before or after T0.

## Go/no-go sign-off

| Record | Value |
|---|---|
| All rows verified | ☐ yes ☐ no |
| Active pause/incident | ☐ none ☐ present |
| Overall decision | ☐ go ☐ no-go |
| Founder/owner | ____________________ |
| Technical reviewer | ____________________ |
| Privacy/legal reviewer | ____________________ |
| Decision timestamp | ____________________ |
| Evidence index | ____________________ |
| Next review | ____________________ |

If the decision is go, set T0 to the first Monday after all rows passed, using
[30-DAY-CALENDAR.md](./30-DAY-CALENDAR.md). On T0, the first campaign record is
one real prospect processed end to end under the
[SOP checkpoint](./SOP.md#t0-first-real-prospect-checkpoint). It counts in the
150; pause after it to confirm the controls, then continue only within the
authorised daily cap. This live checkpoint is not a circular launch-gate row.
Any later failed gate or pause threshold returns the campaign to no-go.
