# Handoffs

Create one new file per completed or paused task:

`YYYY-MM-DD-HHMM-<agent>-<task>.md`

Use `../HANDOFF-TEMPLATE.md`. Files are append-only records: never edit another
agent's handoff to change its conclusions. Add a newer handoff if later
verification supersedes earlier evidence.
