# Handoff — week 5 article: Cyber Essentials self-assessment questions

- **Agent:** Claude
- **Date:** 27 August 2026
- **Branch:** `claude/seo-week-5`
- **Worktree:** `.worktrees/seo-week-5`
- **Commit:** `bea0d02`
- **Base:** `main` at `cc9b12e`

## Task

Publish the week 5 article in `docs/seo/ORGANIC-SEARCH-EXECUTION.md`, after the
owner chose to promote the self-assessment target over the planned CE Plus
audit article.

## Why the target changed

`claude/seo-measure-sort` fixed a ranking bug in `scripts/seo-measure.mjs` that
had been returning the alphabetically-first queries as if they were the top
ones. With the full table visible, a self-assessment cluster of ~46 impressions
appeared, three of its queries already at position 26–35 with no page written
for them:

| Query | Impressions | Avg position |
|---|---:|---:|
| cyber essentials questionnaire | 12 | 88.5 |
| cyber essentials readiness assessment | 9 | 26.3 |
| cyber essentials self assessment | 8 | 32.1 |
| cyber essentials self assessment questionnaire | 5 | 61.0 |
| cyber essentials self-assessment | 4 | 31.5 |
| iasme cyber essentials questionnaire | 4 | 75.3 |
| cyber essentials readiness check | 4 | 78.3 |

The calendar had deferred this article to week 13 on the stated grounds of "no
measured demand yet" — a premise that came from the truncated table.

## Changes

- `src/app/(marketing)/blog/cyber-essentials-assessment-questions/page.tsx` (new)
- `src/lib/seo/registry.ts` — new `ARTICLES.cyberEssentialsAssessmentQuestions`
  entry, which feeds the blog index, the sitemap and article structured data
- `public/llms.txt` — added to the Guides list
- `docs/seo/ORGANIC-SEARCH-EXECUTION.md` — week 5 replaced, weeks 5–12 all slide
  one week, CE Plus cluster now weeks 6–8, nothing dropped, and a dated section
  recording why

## Editorial constraint

The Danzell question set belongs to IASME and is **not reproduced**. The article
is built around what each part of the assessment needs from the organisation and
who in the business holds it, and it states the reason explicitly: a copied
question list goes stale the day the set changes, as it did on 27 April 2026.
The IASME preview page is linked as the authoritative source.

## Verification

```
npx tsc --noEmit                    # clean
npx eslint <page> src/lib/seo/registry.ts   # clean
npx vitest run                      # 326 passed, 1 failed
npx next build                      # succeeds; route prerenders static (○)
```

The single failure is `pdf-primitives.test.tsx`, the known worktree artifact:
`scripts/prepare-report-fonts.mjs` builds an explicit
`process.cwd()/node_modules/...` path and the worktree has no `node_modules`.
It passes on `main`.

Rendered page checked at `http://localhost:3007/...`: one `<h1>`, eight `<h2>`
sections, `Article` and `FAQPage` JSON-LD both present, canonical correct.
`.env.local` was symlinked into the worktree so the Supabase proxy would start;
the symlink is gitignored under `.env*`.

## External state

None. No production write, no deploy, no external service touched from this
branch.

## Next safe action

Owner merges `claude/seo-measure-sort` then `claude/seo-week-5` into `main`.
Pushing `main` auto-deploys production, so build the merged result first.
