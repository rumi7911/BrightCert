# Agent task handoff

- **Agent:** Claude
- **Date/time (BST):** 20 August 2026, 11:45
- **Task:** Publish the overdue Week 3 article from the organic-search execution
  plan: Cyber Essentials requirements v3.3
- **Branch:** `claude/seo-week-3`
- **Worktree:** `.worktrees/seo-week-3`
- **Base commit:** `a8e6c2c`
- **Status:** Complete on the task branch; not pushed, not deployed. Awaiting
  owner review.

## Why this article

`docs/seo/ORGANIC-SEARCH-EXECUTION.md` schedules it for **17 August** and it was
not written; the hackathon absorbed weeks 3 onward. It is the highest-intent
query in the plan (`cyber essentials requirements`) and the site had only the
four July articles.

It also converges with LinkedIn Post A in
`marketing/linkedin-operational-facts-track.md`, scheduled **Fri 21 August**, on
the same subject. Once this deploys, that post's first comment should link here
rather than to the homepage.

## Scope and ownership

- **Files changed:**
  - `src/app/(marketing)/blog/cyber-essentials-requirements/page.tsx` (new)
  - `src/lib/seo/registry.ts` — one `ARTICLES` entry; extends the existing
    source of truth rather than adding a second, per the 29 July SEO handoff
  - `src/components/brightcert/article-kit.tsx` — published/reviewed dates
    wrapped in `<time dateTime>`. **Shared component: this also affects the four
    existing articles.** Purely semantic; `article-kit.test.tsx` passes.
  - This handoff
- **Files inspected but not changed:** `src/app/sitemap.ts`, `robots.ts`,
  `seo.test.ts`, `json-ld.tsx`, the four existing articles, the questions
  registry.
- **Overlapping work discovered:** none. `main` was level with `origin/main` and
  clean apart from untracked `videos/`.

## Content and sourcing

Every factual claim was verified against BrightCert's own published pages before
drafting, per the claim-discipline rule:

- v3.3 effective 27 April 2026, Danzell replacing Willow — `what-is-cyber-essentials`
- MFA required for users of cloud services where the service makes it available;
  cloud cannot be scoped out — `what-is-cyber-essentials`, `iasme-tool-vs-brightcert`
- Scope covers remote workers and personally owned devices — `what-is-cyber-essentials`
- 14-day window for critical/high-risk updates — `control-accordion.tsx`,
  `src/lib/questions/index.ts:525`
- Five control-area names — `src/lib/questions/index.ts`

**No fee figures appear in this article**, so the FOUNDING10 cap wording is not
triggered. The official Danzell question set is not reproduced. No pass
guarantee, no use of "certify" for BrightCert.

Original element required by the brief: a five-row table mapping each control
area to what v3.3 asks and the evidence an SME should collect.

## Pre-publication gate

All ten items in `ORGANIC-SEARCH-EXECUTION.md` pass, verified against the
prerendered HTML in `.next/server/app/blog/cyber-essentials-requirements.html`
rather than from source:

- One `<h1>`; 9 `<h2>`, 6 `<h3>`, no skipped levels
- Self-canonical `https://brightcert.co.uk/blog/cyber-essentials-requirements`
- Unique title and description; `og:type` `article`
- Article, BreadcrumbList and FAQPage JSON-LD all present
- Author link and both dates now emitted as `<time dateTime>`
- Semantic `<table>` with `<caption>`, 3 `scope="col"` and 5 `scope="row"`
  headers, inside an `overflow-x-auto` container
- Seven `<section aria-labelledby>` regions bound to their headings
- `<abbr title>` on first use of MFA
- Hub link, related-articles block, one assessment CTA, disclaimer with NCSC and
  IASME primary sources
- Present in `sitemap.xml`; appears on `/blog` automatically via `ARTICLES`

## Verification

- `npx tsc --noEmit` — clean
- `npm run lint` — clean
- `npm run build` — clean; route prerendered as static (`○`)
- `npm run test:run` under **Node 20** — **324 passed, 1 failed**

The single failure is the documented fresh-worktree gotcha:
`pdf-primitives.test.tsx` runs `scripts/prepare-report-fonts.mjs`, which builds
an explicit `process.cwd()/node_modules/@fontsource/...` path that does not exist
in a worktree. Unrelated to this change; passes on `main`.

**Node 26 breaks the suite independently of this work.** Under the default
`node v26.7.0`, `src/lib/analytics/consent.test.ts` fails all 10 tests with
`localStorage` undefined in jsdom. Confirmed identical on `main`, so it is
environmental, not a regression. Use
`/opt/homebrew/Cellar/node@20/20.20.2/bin` — the same Node 20 path already noted
for this project.

## What the owner still controls

- Reviewing the copy and the reviewed date
- Pushing the branch, merging and deploying
- Requesting reindexing in Search Console after deploy
- Deciding whether LinkedIn Post A on 21 August links here (recommended) or to
  the homepage

## Next in the plan

Week 4, due **Mon 24 August**: `/blog/cyber-essentials-checklist`. Brief is
already written in `ORGANIC-SEARCH-EXECUTION.md`; the original element required
is a printable dated checklist with owner, evidence and status columns.

Separately, the weekly scorecard in that document has never been filled in and
there is no measurement tooling — the 11 August baseline was taken ad hoc. A
`npm run seo:measure` script is the obvious next piece of infrastructure; the
method, including the sandbox constraint on `google-auth-library`, is recorded in
`docs/growth/SEO-BASELINE-2026-08-11.md`.
